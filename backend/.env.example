DATABASE_PATH=./data/jobs.db
FAISS_INDEX_PATH=./data/jobs.faiss
EMBEDDING_MODEL=all-MiniLM-L6-v2
CORS_ORIGINS=http://localhost:5173
# Gemini keys are supplied per-request via the X-Gemini-API-Key header.
# This value is only used by scripts/local testing and is never required in production.
GEMINI_API_KEY=
